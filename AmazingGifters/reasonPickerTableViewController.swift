//
//  reasonPickerTableViewController.swift
//  AmazingGifters
//
//  Created by Chong Wu Guo on 7/6/16.
//  Copyright © 2016 Kai Lu. All rights reserved.
//

import UIKit

class reasonPickerTableViewController: UITableViewController {
    /*
    var reason: String?
    var reasons: [String] = ["Birthday","Graduation","Father's Day", "Mother's Day","Christmas Day","Thanskgiving Day", "Other"]
 
    
    override func tableView(tableView: UITableView, numberOfRowsInSection section: Int) -> Int
    {
        
        return reasons.count
    }
    override func tableView(tableView: UITableView, cellForRowAtIndexPath indexPath: NSIndexPath) -> UITableViewCell {
        let cellIdentifier = "reasonCell"
        let cell = tableView.dequeueReusableCellWithIdentifier(cellIdentifier, forIndexPath: indexPath) as UITableViewCell
        
        cell.textLabel!.text = reasons[indexPath.row]
        return cell
        
    }
    override func prepareForSegue(segue: UIStoryboardSegue, sender: AnyObject?) {
        if segue.identifier == "reasonSegue"
        {
            if let destinationVC = segue.destinationViewController as? AddGiftTableViewContonller {
                if let cell = sender as? UITableViewCell, let indexPath = tableView.indexPathForCell(cell) {
                    let text = cell.textLabel!.text
                    destinationVC.reasonLabel1 = text!
                                        
                }
            }
        }
    }
 */
}